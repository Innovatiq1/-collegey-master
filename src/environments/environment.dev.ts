import { DEFAULT_CONFIG } from '../configs/dev';

export const environment = {
  apiEndpoint: DEFAULT_CONFIG.apiEndpoint,
  frontEndUrl: DEFAULT_CONFIG.frontEndUrl,
 
  production: false,
  awsUrl: DEFAULT_CONFIG.awsUrl,
};
